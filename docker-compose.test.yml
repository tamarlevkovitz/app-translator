services:
  backend-test:
    build: ./backend
    volumes:
      - ./integration.test.mjs:/app/integration.test.mjs
    command: sh -c "sleep 5 && node --test /app/integration.test.mjs"
    environment:
      DATABASE_URL: postgres://postgres:postgres@db-test:5432/translations_test
    working_dir: /app
    depends_on:
      db-test:
        condition: service_healthy
    networks:
      - test-network

  db-test:
    image: postgres:15
    environment:
      POSTGRES_DB: translations_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    networks:
      - test-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 5s
      retries: 10

networks:
  test-network:
    driver: bridge